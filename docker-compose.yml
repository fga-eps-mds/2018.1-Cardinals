version: '3'

services:
  db:
    container_name: cardinalsdb
    image: postgres
  web:
    container_name: cardinalsweb
    build: .
    volumes:
      - .:/code
    ports:
      - "8000:8000"
    depends_on:
      - db
    env_file:
      - .env
